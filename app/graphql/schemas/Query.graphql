type Options {
  district: [District]
  station: [Station]
}


type Query {
  room(id: String!): Room
  rooms(
    name: String,
    minPrice: Int,
    maxPrice:Int,

    limit: Int,
    offset: Int
  ): Rooms
  district(id: Int): [District]
  options: Options
}